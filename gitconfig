[user]
  name = Rafael Rinaldi
  email = rafael@rinaldi.io

[github]
  user = rafaelrinaldi

[hub]
  protocol = https

[alias]
  branches = branch -a
  bypass = update-index --assume-unchanged
  c = commit -v
  co = checkout
  diff = diff --color-words --ignore-space-at-eol
  l = log --pretty='format:%C(yellow)%h %C(blue)%ad %C(reset)%s%C(red)%d %C(green)%an%C(reset), %C(dim)%ar' --date=short
  ls = ls-tree --full-tree -r HEAD
  please = push --force-with-lease
  remotes = remote -v
  s = status -sb
  tags = tag -l
  undo = reset --soft HEAD^1
  what = whatchanged --decorate

[mergetool "Kaleidoscope"]
  cmd = ksdiff --merge --output \"$MERGED\" --base \"$BASE\" -- \"$LOCAL\" --snapshot \"$REMOTE\" --snapshot
  trustexitcode = true

[merge]
  tool = Kaleidoscope
  summary = true

[difftool "Kaleidoscope"]
  cmd = ksdiff --partial-changeset --relative-path \"$MERGED\" -- \"$LOCAL\" \"$REMOTE\"

[difftool]
  prompt = false

[mergetool]
  prompt = false

[diff]
  tool = Kaleidoscope

[core]
  ; Start Vim in insert mode
  editor = vim +start
  whitespace = cr-at-eol

[commit]
  template = ~/.gitmessage

[color]
  ui = auto
  branch = auto

[color "diff"]
  meta = yellow
  frag = magenta
  old = red bold
  new = green bold

[color "status"]
  added = magenta
  changed = green
  untracked = cyan

; Use `origin` as the default remote on the `master` branch in all cases
[branch "master"]
  remote = origin
  merge = refs/heads/master

[fetch]
  prune = true

[push]
  default = current

[rebase]
  autosquash = true
